:queues:
  - default
  - mailers
  - monitor
:schedule:
  collect_stats:
    every: 1m   # Runs once per minute
    class: CollectStatsWorker
  schedule_monitor:
    every: 1m
    class: ScheduleMonitorWorker
  check_radio_is_up:
    every: 10m
    class: CheckRadioIsUp
production:
  :concurrency: 5
