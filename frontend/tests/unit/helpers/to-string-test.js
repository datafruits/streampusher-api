import { toString } from 'frontend/helpers/to-string';
import { module, test } from 'qunit';

module('Unit | Helper | to string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toString([42]);
  assert.ok(result);
});
