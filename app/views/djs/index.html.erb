<%= render 'form' %>
<%= render 'search_form' %>
<%= paginate @djs %>
<table class="table table-striped">
  <thead>
    <th>username</th>
  </thead>
  <tbody>
    <% @djs.each do |dj| %>
      <tr>
        <td><%= dj.username %></td>
        <td><%= dj.email  %></td>
        <td><%= dj.time_zone %></td>
        <% if current_user.owner? %>
          <td><%= link_to 'edit', edit_dj_path(dj) %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
<%= paginate @djs %>
