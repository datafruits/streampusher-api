<h1>podcasts</h1>
<div class="row">
  <%= render 'form' %>
</div>

<table class="table table-striped">
  <thead>
    <th>name</th>
    <th>playlist</th>
    <th>XML feed</th>
  </thead>
  <tbody>
    <% @podcasts.each do |podcast| %>
      <tr>
        <td><%= podcast.name %></td>
        <td><%= podcast.playlist.try(:name) %></td>
        <td><%= link_to 'xml feed', podcast_path(id: podcast.name, format: :xml) %></td>
        <td><%= link_to 'Edit', edit_podcast_path(podcast) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
