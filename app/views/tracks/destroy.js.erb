$("ul#tracks li.track[data-track-id=<%=@track.id%>]").fadeOut(300, function(){
  $(this).remove();
});
$("ul.playlist-tracks li.track[data-track-id=<%=@track.id%>]").fadeOut(300, function(){
  $(this).remove();
});
